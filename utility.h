#include <cmath>
#include "raylib.h"

// parameters are the vec2 of the objects
float Get_Angle(Vector2 to_object, Vector2 from_object);
float Get_Rotation(float angle);
float Direction_To(Vector2 to_object, Vector2 from_object);  
